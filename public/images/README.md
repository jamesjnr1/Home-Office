Drop the real storefront photos here with these exact filenames — the
site already references them:

- `storefront-1.jpg` — main exterior/entrance photo (used largest, in the
  hero and About page)
- `storefront-2.jpg` — second photo (used alongside the first)

Any reasonably sized JPG works (a few hundred KB to ~1-2MB is plenty for
web use — no need to upload full camera-resolution originals).
